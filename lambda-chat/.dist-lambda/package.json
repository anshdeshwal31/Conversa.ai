{
    "name": "chat-reset-lambda",
    "version": "1.0.0",
    "type": "module",
    "dependencies": {
        "@prisma/client": "^5.0.0",
        "prisma": "^5.0.0"
    },
    "devDependencies": {
        "dotenv-cli": "^8.0.0",
        "typescript": "^5.9.2"
    },
    "scripts": {
        "prisma:generate": "dotenv -e ../.env -- prisma generate --schema=./prisma/schema.prisma",
        "prisma:migrate": "dotenv -e ../.env -- prisma migrate deploy --schema=./prisma/schema.prisma",
        "build": "npx tsc lambda-chat-reset.ts --target ES2022 --module NodeNext --moduleResolution NodeNext --outDir .",
        "typecheck": "npx tsc --noEmit lambda-chat-reset.ts --target ES2022 --module NodeNext --moduleResolution NodeNext"
    }
}